{
  "name": "ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5173",
    "build": "tsc && vite build --mode pad && vite build --mode welcome && vite build --mode timeslider",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/jquery": "^4.0.0",
    "@types/node": "^25.3.3",
    "esbuild": "0.27.3",
    "typescript": "^5.6.3",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "@rollup/plugin-commonjs": "^29.0.0",
    "socket.io-client": "^4.8.3",
    "underscore": "^1.13.8",
    "unorm": "^1.6.0"
  }
}
