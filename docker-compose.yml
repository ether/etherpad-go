services:
  postgres:
    image: postgres:18-alpine
    container_name: postgres-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
      POSTGRES_DB: mydb
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U myuser -d mydb || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
  etherpad:
    image: etherpad/etherpad:latest
    container_name: etherpad
    restart: unless-stopped
    environment:
      - ETHERPAD_DBSETTINGS_TYPE=postgres
      - ETHERPAD_DBSETTINGS_HOST=postgres
      - ETHERPAD_DBSETTINGS_PORT=5432
      - ETHERPAD_DBSETTINGS_NAME=mydb
      - ETHERPAD_DBSETTINGS_USER=myuser
      - ETHPERPAD_DBSETTINGS_PASSWORD=mypassword
    ports:
      - "9001:9001"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9001/health"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  postgres-data:
